export class Task {
  constructor(public id?: number,
              public idCategory?: number,
              public title?: string,
              public isDone?: boolean,
              public description?: string) {
  }
}
